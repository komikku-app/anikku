org.gradle.caching=true
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

kotlin.code.style=official

# ---- App Version (override with -Pversion=X.Y.Z) ----
appVersion=1.8.5
appVersionName=1.8.5

# ---- Subtitle provider credentials (baked into the app) -------------------
# Free keys so end users don't configure anything:
#   - Jimaku token:     https://jimaku.cc/account  (free)
#   - OpenSubtitles:    https://opensubtitles.com API consumer + free account
# Keys are baked into the release binary. Users can still override them in
# Settings (stored in the Keychain). Note: all users share these quotas
# (Jimaku free tier + OpenSubtitles ~10 downloads/day per account).
subtitle.jimakuToken=AAAAAAAAMuYuAS5jxKf38yxKwrZgFhkPgwNWSu1Mavgr6DenEaB7B2wYdA
subtitle.opensubtitles.apiKey=9OM04s4sjUjQ0imkDaXSp47q4p3uGw5X
subtitle.opensubtitles.username=ernestadmin
subtitle.opensubtitles.password=123qwe123

# ---- AniList OAuth client (baked into the app) ----------------------------
# Register an app at https://anilist.co/settings/developers and paste the
# client id/secret here to make tracker login one click. Empty values fall
# back to manual credential entry in Settings > Manage Trackers, so the app
# works without them.
anilist.clientId=47764
anilist.clientSecret=wvAQIlb9UEVo6X7y3dzVEv7srZK19uQR8fEaalSh

# Homebrew JDK is the only one installed here; the vendor check is not
# relevant for our ad-hoc packaging flow.
compose.desktop.packaging.checkJdkVendor=false
